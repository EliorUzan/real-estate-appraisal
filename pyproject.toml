[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "real-estate-appraisal-assistant"
version = "0.1.0"
description = "Collaborative Hebrew real-estate appraisal web application"
requires-python = ">=3.11"
dependencies = [
    "PySide6>=6.7,<7",
    "pypdf>=5.0",
    "python-docx>=1.1",
    "openpyxl>=3.1",
    "xlrd>=2.0",
    "google-genai>=1.0",
    "openai>=1.0",
    "anthropic>=0.50",
    "keyring>=25.0",
    "fastapi>=0.115,<1",
    "uvicorn[standard]>=0.30,<1",
    "pydantic-settings>=2.6,<3",
    "PyJWT[crypto]>=2.10,<3",
    "cryptography>=43,<46",
    "supabase>=2.10,<3",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "httpx>=0.27"]

[project.scripts]
appraisal-assistant = "appraisal_assistant.main:main"
appraisal-api = "api.main:run"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"appraisal_assistant.agents.prompts" = ["*.md"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
