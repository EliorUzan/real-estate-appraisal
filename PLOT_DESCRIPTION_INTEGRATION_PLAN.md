# תוכנית אינטגרציה וארכיטקטורה: הפקת פרק "תיאור החלקה" (סעיף 7.1) באמצעות GovMap API

מסמך זה מרכז את האפיון, הניתוח הטכנולוגי ותוכנית האינטגרציה לפיתוח סוכן **"תיאור החלקה" (Plot Description)** במסגרת מערכת הבינה המלאכותית לשמאי מקרקעין. סעיף זה מהווה את ההרחבה הטבעית לפרק "תיאור הסביבה" שכבר פעיל במערכת.

---

## 1. רקע והסבר טכני לגבי סריקת התיעוד (`content.md`)

במהלך שלב האפיון והמחקר הטכנולוגי מול פורטל ה-API של המרכז למיפוי ישראל (`api.govmap.gov.il`), בוצעו מספר פניות לשליפת דפי תיעוד רשמיים באמצעות כלי `read_url_content`.
כלי זה מוריד את עמודי ה-HTML משרת התיעוד וממיר אותם לקובץ Markdown מקומי לצורך קריאה וניתוח. כברירת מחדל, הקובץ שנוצר בכל קריאה נשמר תחת השם **`content.md`** בתיקיית צעד זמנית נפרדת.
באמצעות פניות אלו נסרקו ונותחו הפרקים הבאים בתיעוד הרשמי של GovMap:
- **פונקציות חיפוש ואיתור:** `searchAndLocate`, `search`, `getSearchResultData`.
- **שירותי חיתוך ומרחב עצמאיים:** `getLayerFeaturesByLocation`, `intersectFeatures`.
- **נספח שכבות רשמי (Attachment A):** איתור השכבות `PARCEL_ALL` (חלקות ושטח רשום), `retzefMigrashim` (מגרשי תב"ע של רמ"י לקבלת ייעודי קרקע גובלים), `ways` ו-`STREET_ALL` (רשת דרכים ושמות רחובות), ו-`BUILDINGS` (פוליגונים של מבנים פיזיים).
- **נספח טיפוסי גיאומטריה וקואורדינטות (Attachment B):** מבנה נתוני WKT, גיאומטריות ברשת ישראל החדשה (ITM / EPSG:2039), ומבני התשובה של ה-REST API.

---

## 2. מפרט הפלט השמאי הנדרש (סעיף 7.1)

הפלט המבוקש מבוסס על מבנה תקני של דוחות שומה בישראל (כפי שמופיע בשומת הייחוס מכפר חסידים, גוש 11140 חלקה 91):

```text
7. תיאור החלקה, המבנה והנכס:
7.1 תיאור החלקה:
חלקה 91 בגוש 11140 בשטח רשום של 1,003.00 מ"ר. 
הקרקע מישורית.
הצורה הגיאומטרית של החלקה מעין מלבנית.
החלקה גובלת בחלקות הבאות:
מצפון- חלקה ביעוד דרך (רחוב נחל קדומים).
ממערב- חלקה ביעוד מגורים.
מדרום- חלקה ביעוד קרקע חקלאית. 
ממזרח- חלקה ביעוד מגורים.
החלקה כוללת שני מבני מגורים.
הערה: בפיצול יש לאפשר דרך גישה לרכב למגרש העורפי ככול שתפוצל החלקה.
```

---

## 3. מיפוי מרכיבי הסעיף מול מקורות המידע והאלגוריתמיקה

| סעיף / שורה בטקסט השומה | מקור המידע | שכבה / פונקציה ב-GovMap API | עיבוד נדרש במערכת |
| :--- | :--- | :--- | :--- |
| **זיהוי החלקה והגוש**<br>`חלקה 91 בגוש 11140` | GovMap Cadastre API / קלט השמאי | שכבת `PARCEL_ALL`<br>שדות `GUSH_NUM`, `PARCEL` | אימות גוש וחלקה מול כתובת או הזנה ישירה. |
| **שטח רשום**<br>`בשטח רשום של 1,003.00 מ"ר` | GovMap Cadastre API / נסח טאבו | שכבת `PARCEL_ALL`<br>שדה `LEGAL_AREA` | הצגת שטח רשום בפורמט מספר עם פסיקים ושתי ספרות עשרוניות. |
| **טופוגרפיה**<br>`הקרקע מישורית` | נתוני מודל גבהים (DEM) / קלט שמאי | שכבת קווי גובה או קלט משתמש ישיר | חישוב משרעת גבהים בפוליגון: עד 2%-3% שיפוע יוגדר כ"מישורית", מעבר לכך "משופעת" / "מדרונית". |
| **צורה גיאומטרית**<br>`הצורה הגיאומטרית מעין מלבנית` | גיאומטריית WKT מ-GovMap | פוליגון החלקה מ-`PARCEL_ALL` | אלגוריתם Minimum Oriented Bounding Box (OBB): יחס כיסוי > 0.85 ו-4 קודקודים יוגדר כ"מעין מלבנית"; זוויות אלכסוניות יוגדר כ"טרפזית" / "רב-צלעית". |
| **גבולות החלקה לפי כיוונים**<br>`מצפון- חלקה ביעוד דרך...`<br>`ממערב- חלקה ביעוד מגורים` | חיתוך מרחבי (Spatial Buffer + Intersect) | 1. `getLayerFeaturesByLocation` / `intersectFeatures`<br>2. `retzefMigrashim` (ייעודי תב"ע ברמ"י)<br>3. `ways` / `STREET_ALL` (שמות רחובות) | חישוב אזימוט/וקטור ממרכז החלקה לקביעת רוחות השמיים (צפון, דרום, מזרח, מערב) וחילוץ ייעוד התב"ע ושם הרחוב. |
| **מבנים קיימים בחלקה**<br>`החלקה כוללת שני מבני מגורים` | חיתוך מרחבי של מבנים | שכבת `BUILDINGS` (פוליגונים של מבנים) | ספירת פוליגונים של מבנים המוכלים בתוך שטח החלקה (Contains/Overlaps). |
| **הערה תכנונית / דרכי גישה**<br>`בפיצול יש לאפשר דרך גישה...` | ניתוח טופולוגי של חזיתות הדרך | שילוב נתוני הגבולות ושכבת הדרכים | זיהוי עובדתי שחזית לדרך קיימת מפאה אחת בלבד (צפון), בעוד שאר הפאות גובלות במגרשים פרטיים – הפקת הערת התראה תכנונית לשמאי. |

---

## 4. מודל הנתונים וחוזים (Pydantic Data Contracts)

לצורך העברת נתונים נקייה ומהימנה בין שכבת ה-API והחישוב המרחבי לבין סוכן ה-LLM, מוגדרים החוזים הבאים:

```python
from pydantic import BaseModel, Field
from typing import List, Optional

class AdjacentBorder(BaseModel):
    direction: str = Field(..., description="כיוון רוח השמיים: מצפון, מדרום, ממזרח, ממערב")
    land_use: str = Field(..., description="ייעוד הקרקע מתוך התב״ע: דרך, מגורים, קרקע חקלאית, שב״צ וכו'")
    street_name: Optional[str] = Field(None, description="שם הרחוב במידה והייעוד הוא דרך")
    adjacent_gush_parcel: Optional[str] = Field(None, description="מספר גוש/חלקה גובלת ככל שקיים")

class PlotAnalysisData(BaseModel):
    gush: int = Field(..., description="מספר גוש")
    parcel: int = Field(..., description="מספר חלקה")
    legal_area: float = Field(..., description="שטח רשום במ\"ר (מתוך PARCEL_ALL או נסח)")
    topography: str = Field(default="מישורית", description="מאפיין טופוגרפי: מישורית, משופעת, מדרונית")
    geometry_shape: str = Field(..., description="סיווג הצורה הגיאומטרית: מעין מלבנית, טרפזית, רבועה, רב-צלעית")
    borders: List[AdjacentBorder] = Field(..., description="פירוט גבולות החלקה לארבע רוחות השמיים")
    buildings_count: int = Field(default=0, description="מספר מבנים פיזיים שזוהו בתחום החלקה")
    buildings_use: Optional[str] = Field(default="מגורים", description="ייעוד המבנים (מגורים, מסחר, מבנה עזר)")
    planning_notes: Optional[str] = Field(None, description="הערות תכנוניות הנובעות מחזיתות הדרך או אילוצי גישה")
    appraiser_warnings: List[str] = Field(default_factory=list, description="אזהרות ואי-התאמות לבדיקת השמאי")
```

---

## 5. אלגוריתמיקה מרחבית (Spatial Geometry Analysis)

המערכת לא תסתמך על ניחוש של מודל השפה לקביעת צורת החלקה או כיווני השכנים, אלא תבצע חישוב גיאומטרי דטרמיניסטי:

1. **סיווג צורת החלקה (Shape Classification):**
   - קבלת קואורדינטות פוליגון החלקה מ-`PARCEL_ALL`.
   - חישוב תיבה חוסמת מוטה מינימלית (Minimum Oriented Bounding Box - OBB).
   - חישוב יחס השטחים (Intersection Area / OBB Area):
     - יחס $\ge 0.88$ עם 4 קודקודים עיקריים $\rightarrow$ "מעין מלבנית".
     - יחס בין 0.75 ל-0.88 עם צלעות מתכנסות $\rightarrow$ "טרפזית".
     - יחס נמוך יותר או מעל 5 קודקודים משמעותיים $\rightarrow$ "פוליגונלית / רב-צלעית בלתי סדירה".

2. **קביעת כיווני הגבולות (Directional Azimuths):**
   - חישוב נקודת המרכז (Centroid) של פוליגון החלקה.
   - חלוקת המרחב סביב המרכז לפי גזרות אזימוט:
     - $315^\circ - 45^\circ \rightarrow$ צפון.
     - $45^\circ - 135^\circ \rightarrow$ מזרח.
     - $135^\circ - 225^\circ \rightarrow$ דרום.
     - $225^\circ - 315^\circ \rightarrow$ מערב.
   - חיתוך הפוליגונים הגובלים (`retzefMigrashim` ו-`ways`) ושיוך כל שכן לגזרה המתאימה.

3. **זיהוי חזית לדרך והתראת גישה:**
   - בדיקה האם ייעוד "דרך" מופיע רק בפאה אחת מתוך ארבע הפאות.
   - אם מופיע בפאה אחת בלבד והחלקה מיועדת למגורים צמודי קרקע / מאפשרת פיצול, המערכת מייצרת את ההערה התכנונית הסטנדרטית בדבר הבטחת דרך גישה למגרש עורפי.

---

## 6. סוכן ההפקה (Prompt Architecture)

בדומה לסוכן תיאור הסביבה הקיים (`environment_description.md`), סוכן תיאור החלקה מונחה לפעול תחת כללים מקצועיים נוקשים:
- קובץ הפרומפט ממוקם בנתיב המיועד: `src/appraisal_assistant/agents/prompts/plot_description.md`.
- הקובץ כולל הנחיות תפקיד, מבנה היררכי, סגנון כתיבה שמאי, כללי זהירות מחמירים למניעת הזיות (Hallucinations), ודוגמאות אימון מדויקות משומות אמת.
- מודול Python נלווה (`plot_description.py`) טוען את קובץ ה-Markdown ומספק פונקציית עזר להרכבת הודעת המשתמש עם העובדות המאומתות שנשלפו מ-GovMap.

---

## 7. שלבי יישום בפרויקט (Roadmap)

1. **שלב א' (הושלם):**
   - אפיון מקורות המידע והשכבות ב-GovMap API.
   - יצירת מסמך תוכנית האינטגרציה בשורש הפרויקט (`PLOT_DESCRIPTION_INTEGRATION_PLAN.md`).
   - יצירת קובץ הפרומפט הראשוני של הסוכן (`plot_description.md`).

2. **שלב ב' - מודולי שירות וגיאומטריה:**
   - בניית `govmap_client.py` תחת שכבת ה-Infrastructure לשליפת נתוני גוש/חלקה ושכבות מרחביות.
   - בניית `geometry_service.py` לניתוח גיאומטרי של צורת החלקה, כיווני רוחות השמיים וחיתוך שכבות.

3. **שלב ג' - אינטגרציה ב-API וב-UI:**
   - הוספת מודל בקשה ותשובה ל-FastAPI (`/api/v1/sections/plot-description`).
   - הוספת מסך / לשונית "תיאור החלקה" בממשק המשתמש של המערכת.
   - בדיקות יחידה ואימות מול שומת כפר חסידים.

---

## 8. סטטוס אינטגרציה ומגבלות שאומתו (24.9.2026)

ממשק ה-Web כולל כעת מסלול עצמאי לסעיף "תיאור החלקה" ללא שימוש ב-AI. הוא מאתר כתובת מדויקת באמצעות GovMap, שולף מועמדי גוש/חלקה משכבת `PARCEL_ALL`, מנסה לקבל גם את `LEGAL_AREA` ומאפשר לשמאי לבחור חלקה במקרה של מספר התאמות. המערכת מפיקה טיוטה דטרמיניסטית ומאפשרת להוריד את הנתונים המובנים כ-JSON לשימוש עתידי בסוכן. הנתונים שהשמאי לא אימת מושמטים מהטיוטה.

יש להבחין בין **שטח קדסטרי המוצג במפה** לבין **שטח רשום לפי נסח**: אתר איתור גוש/חלקה הרשמי של מפ"י מציין שנתוניו אינם אסמכתא, ולשטח רשום בעל תוקף משפטי יש לפנות ללשכת המרשם. לכן שדה `LEGAL_AREA`, גם אם GovMap מחזיר אותו, אינו מוזן אוטומטית בטיוטה כ"שטח רשום".

בתיעוד הרשמי של `getLayerFeaturesByLocation` תשובת הדוגמה כוללת מאפיינים מזהים, אך אינה כוללת גיאומטריית פוליגון. לפיכך אין בשלב זה בסיס מאומת לחישוב צורה, גבולות לפי כיוונים, שכנים, מספר מבנים בתוך החלקה, טופוגרפיה או הערת פיצול אוטומטית. פרטים אלה מוזנים רק לאחר אימות שמאי. בפרט, ערכי ברירת המחדל "מישורית", "מעין מלבנית", "מגורים" ו-0 מבנים במודל המוצע לעיל אינם קבילים כעובדות ללא מקור.

מקורות: [תיעוד `intersectFeatures`](https://api.govmap.gov.il/docs/javascript-functions/intersect-features), [תיעוד ניתוח מרחבי](https://api.govmap.gov.il/docs/standalone/get-layer-features-by-location), [איתור גוש חלקה וכתובת של מפ"י](https://www.gov.il/apps/mapi/parcel_address/parcel_address.html).
